#comment
box : wouter/empty
services: 
  - wercker/mongodbasdadsas
  - wouter/empty

build:
  steps:
    - npm-install
    - script:
        name: apt
        code: |
          sudo apt-get update
          sudo apt-get install memcached
        
