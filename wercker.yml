#comment
box : wouter/empty
services: 
  - wercker/mongodbasdadsas
  - wouter/empty

build:
  steps:
    - script:
        name: apt
        code: |
          sudo apt-get update
          sudo apt-get install memcached
        
